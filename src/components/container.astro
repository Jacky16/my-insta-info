---
import { cn } from "@/lib/utils";

type Props = {
  class?: string;
};
const { class: className } = Astro.props;
---

<div class={cn(className, "container max-w-7xl mx-auto px-2 md:px-0")}>
  <slot />
</div>
