---
import { FaArrowDown } from "react-icons/fa6";
import { Button, buttonVariants } from "./ui/button";
---

<section
  class="relative w-full h-dvh flex flex-col items-center overflow-hidden"
>
  <div
    class="w-full h-full flex flex-col items-center justify-center gap-6 md:max-w-5xl"
  >
    <h1
      class="text-pretty flex flex-col items-center text-4xl md:text-6xl text-center md:leading-loose z-10 font-semibold text-gray-800"
    >
      Consulta y Descubre las Estadísticas de Tu Perfil de Instagram
    </h1>
    <p class="text-gray-600 text-center">
      Conoce toda la información de tu cuenta de instagram en un solo lugar
    </p>

    <div class="flex justify-center flex-wrap">
      <Button className="rounded-full flex items-center gap-2" size="lg">
        Consultar métricas
      </Button>

      <a
        href="/"
        class={buttonVariants({
          variant: "link",
          size: "lg",
          className: "cursor-pointer",
        })}
      >
        Ver como conseguir mis datos
      </a>
    </div>
  </div>
  <FaArrowDown className="size-8 my-12 animate-bounce" />
</section>
